ИНСТРУКЦИЯ ПО УСТАНОВКЕ
========================

В этом архиве один файл — inflow_scanner_v4_full.py.
Это ПОЛНАЯ версия бота, все доработки (треугольник v2, ATR-стоп,
подтверждение объёма на пробое, prior_trend_up, RSI-предупреждение,
защита от погони за ценой) уже встроены прямо в этот файл.

Старые отдельные файлы (triangle_v2.py, btc_phase.py, patch_btc_phase.py)
из предыдущих версий больше НЕ НУЖНЫ — вся их логика уже перенесена
внутрь inflow_scanner_v4_full.py. Не подключай их отдельно, иначе
получится дублирование функций.

КАК ПОСТАВИТЬ:
1. Скачай inflow_scanner_v4_full.py из этого архива.
2. Замени этим файлом старый файл на сервере целиком (полная замена).
3. Переменные окружения (TG_TOKEN, TRADES_FILE, SIGNALS_FILE и т.д.)
   не трогай — они не менялись.
4. Перезапусти процесс/деплой.
5. Проверь через /scan.
